# UX/UI specification

- Feature ID: FEATURE-002
- Revision: 3
- Input revisions: requirements revision 2 / SHA-256: 2c47c7e39698354c83bc39999b86044ad18cd2c2ec4f45e2d04c9dec369da933
- Author role: Designer

## Design goals and constraints

- Добавить timed-режим как отдельный режим тренировки, не ломая основной путь FEATURE-001.
- Сделать визуальный контракт для timed-режима максимально понятным за счет реальных макетов ключевых экранов, согласованных пользователем.
- Использовать именно предоставленные пользователем макеты как часть design artifact, а не как факультативное приложение.
- В timed-сессии держать таймер постоянно видимым, но не конкурирующим с математическим выражением.
- После каждого принятого ответа мгновенно переводить пользователя к следующей задаче без промежуточного экрана.
- На timeout однозначно объяснить причину завершения и показать минимум два обязательных результата: сколько решено и сколько правильно.
- Сохранить текущую клавиатуру, правила ввода, mobile-first компоновку и общий дружелюбный детский tone of voice.
- Не использовать мигающие таймеры и не озвучивать countdown каждую секунду.

## Requirement mapping

| Requirement | UX coverage |
|---|---|
| FR-001 | SCREEN-001: явный выбор `Обычный` / `На время` перед стартом |
| FR-002 | SCREEN-002: после каждого submit до дедлайна без паузы показывается следующий пример |
| FR-003 | SCREEN-002 / assumptions: до исчерпания пула задачи не повторяются; UX не требует отдельного управления повторами |
| FR-004 | SCREEN-002: таймер отображает остаток от абсолютного дедлайна |
| FR-005 | SCREEN-002: постоянно видимый countdown `Осталось 0:SS` |
| FR-006 | FLOW-003, SCREEN-003: автоматический переход на результат при timeout |
| FR-007 | FLOW-003: введенный, но не принятый к дедлайну ответ отбрасывается |
| FR-008 | SCREEN-003: обязательные метрики `Правильно X` и `Решено Y`; `Точность Z%` опциональна |
| FR-009 | SCREEN-003: `Еще раз` и `Выбрать класс и режим` |
| FR-010 | FLOW-001: обычный режим сохраняет существующий экран и `N из 10` |
| FR-011 | Accessibility: timer не является ежесекундным live region; timeout объявляется отдельно |
| FR-012 | Видимых analytics controls нет; UX не добавляет персональные данные |

## User journeys

### FLOW-001 Выбор обычного режима

1. Пользователь открывает стартовый экран.
2. Выбирает класс.
3. В блоке `Режим` выбирает `Обычный`.
4. Нажимает `Начать`.
5. Дальнейший UX полностью соответствует FEATURE-001: `1 из 10` ... `10 из 10`, без таймера.

### FLOW-002 Timed-тренировка

1. Пользователь выбирает класс.
2. Выбирает `На время`.
3. Под названием режима видит пояснение `60 секунд · решай столько, сколько успеешь`.
4. Нажимает `Начать`.
5. Открывается SCREEN-002 с первым примером и таймером `Осталось 1:00`.
6. Пользователь вводит ответ и нажимает `Готово` либо Enter.
7. Если ответ принят до дедлайна, текущий пример немедленно заменяется следующим; поле ответа очищается; фокус остается в рабочем сценарии.
8. Пользователь продолжает до timeout независимо от числа уже решенных примеров.

### FLOW-003 Автозавершение

1. В timed-сессии достигается дедлайн.
2. Любое введенное, но не принятое значение перестает быть активным и не учитывается.
3. Интерактивные элементы упражнения становятся недоступны на время перехода.
4. Приложение автоматически показывает SCREEN-003.
5. Screen reader получает единичное сообщение `Время вышло. Правильно X, решено Y.`.

### FLOW-004 Повтор

1. На SCREEN-003 пользователь нажимает `Еще раз`.
2. Стартует новая timed-сессия того же класса с `Осталось 1:00`, счетчики результата начинаются заново.
3. `Выбрать класс и режим` возвращает на стартовый экран без сохранения прошлой сессии.

## Navigation

`SCREEN-001 Start` → (`Обычный`) → существующий FEATURE-001 exercise/result.

`SCREEN-001 Start` → (`На время`) → `SCREEN-002 Timed exercise` → автоматически по timeout → `SCREEN-003 Timed result`.

`SCREEN-003 Timed result` → `Еще раз` → новая `SCREEN-002 Timed exercise`.

`SCREEN-003 Timed result` → `Выбрать класс и режим` → `SCREEN-001 Start`.

Reload на SCREEN-002 ведет в обычное начальное состояние SCREEN-001.

## Screens

### SCREEN-001 Старт / выбор класса и режима

Сохраняется существующая карточка и заголовок `Математический тренажер`.

Порядок элементов:
1. `Выбери класс` и карточки `1 класс`, `2 класс`.
2. Под ними секция `Режим`.
3. Две карточки выбора:
   - `Обычный` — `10 примеров без ограничения времени`;
   - `На время` — `60 секунд · решай столько, сколько успеешь`.
4. Мотивационный информационный блок.
5. Основная кнопка `Начать`.

Поведение:
- каждая пара выбора оформляется как доступная radio-group;
- старт недоступен, пока не выбраны и класс, и режим;
- режим по умолчанию не выбран, чтобы timed-ограничение нельзя было включить случайно;
- при возврате через `Выбрать класс и режим` выбор сбрасывается;
- макет демонстрирует визуальную семью продукта: крупные карточки, дружелюбные иллюстративные иконки, мягкие цвета, карточный mobile-first layout.

#### Mockup

![SCREEN-001 start screen](mockups/start-screen.jpg)

### SCREEN-002 Timed exercise

Карточка упражнения сохраняет знакомую структуру FEATURE-001, но добавляет статусный верхний блок.

Верхняя часть:
- слева — `Решено: N`;
- справа — визуально более заметно `Осталось 0:SS`;
- оба значения находятся в единой status-полосе и остаются видимыми всё время сессии.

Основная часть:
- текущее математическое выражение;
- отдельная большая зона отображения введенного ответа;
- существующая цифровая клавиатура, `Стереть`, `Готово`.

Таймер:
- начальное отображение `Осталось 1:00`;
- далее `0:59` ... `0:01` ... `0:00`;
- никогда не отображает отрицательные значения;
- на последних 10 секундах может получить визуальное усиление только цветом/weight, без мигания и layout shift;
- UI-tick не определяет бизнес-дедлайн.

Переход между примерами:
- после submit expression и answer меняются в той же карточке;
- ответ очищается;
- `Решено: N` увеличивается только после принятого ответа;
- отдельный success/error feedback по правильности не показывается;
- модальных окон и кнопки `Следующий` нет.

Дополнительный нижний мотивационный блок допустим как secondary non-interactive panel.

#### Mockup

![SCREEN-002 timed exercise](mockups/timed-exercise.jpg)

### SCREEN-003 Timed result

Контент:
- header `Математический тренажер`;
- крупная friendly-иллюстрация, визуально маркирующая завершение timed-сессии;
- заголовок `Время вышло!`;
- обязательная карточка `Правильно: X`;
- обязательная карточка `Решено: Y`;
- опциональная вторичная карточка `Точность: Z%`;
- поддерживающий мотивационный текстовый блок;
- кнопка `Еще раз`;
- кнопка `Выбрать класс и режим`.

Для результата `0 / 0` используется нейтральный текст без негативной оценки: `За эту минуту ответы не были отправлены. Попробуй еще раз.`

#### Mockup

![SCREEN-003 timed result](mockups/timed-result.jpg)

## Visual language derived from approved mockups

- Скруглённые крупные карточки с большим внутренним отступом.
- Светлый мягкий фон приложения и белая основная карточка.
- Яркие детские акцентные цвета: синий как основной action color, зелёный для позитивного статуса, жёлтый/розовый/голубой как декоративные акценты.
- Дружелюбные персонажные иконки и иллюстрации (цифры, таймер, звезда), поддерживающие мотивационный характер интерфейса.
- Крупная типографика для примера, ответа и числовых результатов.
- CTA-кнопки full-width, primary — заполненная синяя, secondary — обводка.

## Component behavior

### MODE-SELECTOR-001

- Radio group `Режим тренировки`.
- Options: `ordinary`, `timed`.
- Визуально использует тот же паттерн selectable cards, что и выбор класса.
- Полностью управляется клавиатурой и имеет `aria-checked`.

### TIMER-001

- Видим только для timed.
- Отображает `Осталось M:SS`.
- Имеет программное имя для assistive technology, но не `aria-live` на каждое изменение.
- Последние 10 секунд: допустимо одноразовое announcement `Осталось 10 секунд`, если реализация не создаёт дублей; это enhancement, не acceptance requirement.

### SOLVED-COUNTER-001

- Отображает число принятых ответов `Решено: N`.
- Не является live region.

### KEYPAD-001

- Поведение без изменений относительно FEATURE-001.
- После timeout кнопки больше не принимают ввод.

### RESULT-STATS-001

- Два самостоятельных обязательных показателя `Правильно` и `Решено`.
- Не использовать формат `X из 10` в timed-режиме.
- `Точность` может присутствовать как secondary metric, но не заменяет два обязательных показателя.

## Content

Основные строки:
- `Выбери класс`
- `Режим`
- `Обычный`
- `10 примеров без ограничения времени`
- `На время`
- `60 секунд · решай столько, сколько успеешь`
- `Решено: 0`
- `Осталось 1:00`
- `Время вышло!`
- `Правильно: X`
- `Решено: Y`
- `Точность: Z%`
- `Еще раз`
- `Выбрать класс и режим`

Допустимы поддерживающие мотивационные слоганы в стиле утверждённых макетов (`Ты можешь!`, `Быстрее, выше, умнее!`, `Отличная работа!`), если они не создают обязательной зависимости бизнес-логики от конкретного текста.

## Accessibility

- Выбор класса и режима — отдельные `radiogroup` с понятными accessible names.
- Все selectable cards имеют видимое focus state и корректный `aria-checked`.
- Таймер доступен через обычный текст/label, но не обновляет live region ежесекундно.
- Timeout должен вызвать один `role=status` / `aria-live=polite` announcement с итогом.
- Контраст таймера и last-10-seconds state должен соответствовать WCAG AA; цвет не единственный носитель информации.
- Иконки и иллюстрации декоративны; смысловые показатели не должны кодироваться только картинкой.
- Кнопки keypad сохраняют текущие accessible labels.
- При переходе на результат фокус переводится на `Еще раз` либо на заголовок результата согласно существующему focus pattern; screen-reader announcement не дублируется.
- Keyboard submit Enter до дедлайна работает как `Готово`; после дедлайна не принимает ответ.

## Responsive behavior

- Целевой диапазон сохраняет текущий минимум 320 px.
- Макеты отражают mobile-first ширину; desktop адаптация может использовать центрированную карточку без изменения логики.
- SCREEN-001: карточки класса и режима идут вертикально на узких экранах.
- SCREEN-002: status-полоса может перестраиваться в две строки, если иначе страдает место для примера или keypad.
- Таймер не должен менять ширину контейнера при `1:00` → `0:59`; использовать стабильную ширину или tabular numerals.
- SCREEN-003: статистические карточки и CTA сохраняют вертикальный stacking.

## Loading / empty / error / validation / permission states

- Loading: отдельного сетевого loading нет; каталог локальный.
- Start validation: `Начать` disabled/aria-disabled, пока не выбраны класс и режим.
- Start error: используется существующее сообщение `Не удалось начать занятие. Попробуйте позже.`; таймер в этом состоянии отсутствует.
- Empty answer: `Готово` недоступно как в FEATURE-001.
- Timeout: штатный end-state, не error; автоматический переход на SCREEN-003.
- Catalog/next-task technical failure во время timed-сессии: не выдавать обычный результат как будто истекло время; показать отдельное error-state с `Попробовать снова` / `Выбрать класс и режим`.
- Permissions: отсутствуют, приложение не использует auth/roles.
- Reload: возврат на SCREEN-001 без resume banner.

## Prototype / mockup evidence

Данный design artifact включает согласованные пользователем repository-native mockups и рассматривает их как обязательную часть UX/UI specification revision 3.

### SCREEN-001 Start / выбор класса и режима

![Mockup — start screen](mockups/start-screen.jpg)

### SCREEN-002 Timed exercise

![Mockup — timed exercise](mockups/timed-exercise.jpg)

### SCREEN-003 Timed result

![Mockup — timed result](mockups/timed-result.jpg)

## Assumptions

- UX принимает утвержденную requirements-семантику: timed — дополнительный режим, ordinary-поток сохраняется без изменений.
- Предоставленные пользователем макеты являются приоритетным визуальным референсом для FEATURE-002.
- `Точность: Z%` допустима как вторичная метрика, но реализация может опустить её в MVP без нарушения обязательных требований, если это будет отдельно подтверждено на следующих этапах.
- Pixel-perfect перенос иллюстраций и декоративных элементов может быть адаптирован под текущую UI-систему, пока сохраняются структура, приоритеты и ключевой look & feel утвержденных макетов.

## Open questions

- Q-UX-001: Подтвердить, что именно эти три предоставленных пользователем макета являются утверждаемым визуальным контрактом для FEATURE-002.
- Q-UX-002: Подтвердить, что `Точность: Z%` остается вторичной/опциональной метрикой и не должна подменять обязательные `Правильно` и `Решено`.
- Q-UX-003: Подтвердить, что допускается адаптация декоративных иллюстраций под возможности текущей реализации без изменения общей визуальной концепции.
